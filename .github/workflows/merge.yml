name: Merge Drive Videos

on:
  workflow_dispatch:

jobs:
  merge:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Upgrade pip and install dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install moviepy==2.0.3 google-api-python-client google-auth-httplib2 google-auth-oauthlib

      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Verify installations
        run: |
          python -c "import moviepy.editor as m; print('MoviePy version:', m.__version__)"
          ffmpeg -version

      - name: Run merge script
        env:
          GDRIVE_SERVICE_ACCOUNT: ${{ secrets.GDRIVE_SERVICE_ACCOUNT }}
        run: python Youtube.py

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: merged-video
          path: final_merged.mp4
